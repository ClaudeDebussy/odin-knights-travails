class Square {
  constructor(position) {
    position = this.position
    moveNumber = 0;
    previousSquare = null;
    validMovesFromSquare;

    findValidMoves(position)
  }

  findValidMoves(position) {

  }
}


function knightMoves(start, destination) {
  let queue = []
  let pointer = 0
  let currentNode;

  queue.push(start)
  while (queue.length) {
    currentNode = queue[pointer]    
    // Add all valid moves from current node
    pointer++
  }
}